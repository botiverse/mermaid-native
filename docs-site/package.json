{
  "name": "mermaid-native-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev .",
    "docs:generate": "node scripts/generate-compatibility.mjs",
    "docs:build": "npm run docs:generate && vitepress build .",
    "docs:preview": "vitepress preview ."
  },
  "devDependencies": {
    "@xmldom/xmldom": "^0.9.12",
    "vitepress": "1.6.4"
  },
  "dependencies": {
    "@mermaid-js/mermaid-zenuml": "^1.0.1",
    "mermaid": "^12.0.0"
  }
}
